#Update the config_alphafolddb.yaml file, notice the uniprot accession number (with isoform extension) and dir_name (gene_name). 

#Copy the input pdb from the mutatex run the working directory

bash do.sh
